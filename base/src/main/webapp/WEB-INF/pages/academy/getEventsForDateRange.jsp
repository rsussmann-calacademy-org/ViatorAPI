<%@ page language="java" contentType="application/json; charset=UTF-8" pageEncoding="UTF-8"%><%@ taglib prefix="c" uri="http://java.sun.com/jsp/jstl/core" %>
<c:if test="${callback != null}">${callback}(</c:if>{"result": "${result}", "message": "${message}","events": [<c:forEach var="event" items="${events}" varStatus="statusEvent"><c:if test="${statusEvent.index > 0}">,</c:if>
{"id": "${event.eventID}", "name": "${event.eventName}", "startDateTime": "${event.startDateTimeJs}", "endDateTime": "${event.endDateTimeJs}", "typeId": "${event.eventTypeID}", "onSaleDateTime": "${event.onSaleDateTimeJs}", "offSaleDateTime": "${event.offSaleDateTimeJs}", "resourceId": "${event.resourceID}", "userEventNumber": "${event.userEventNumber}", "available": "${event.available}", "status": "${event.status}", "roster": "${event.hasRoster}", "private": "${event.privateEvent}"}</c:forEach>]}<c:if test="${callback != null}">);</c:if>
